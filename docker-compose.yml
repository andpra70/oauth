services:
  oauth-server:
    build:
      context: .
      dockerfile: Dockerfile
    image: docker.io/andpra70/oauth-server:latest
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "9000:9000"
    volumes:
      - ./data/oauth:/app/data
